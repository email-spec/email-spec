== Running email-spec Rails 3 examples application

1. In email-spec directory run `rake build` to generate gem files.
2. Make sure you have latest bundler gem.
3. cd into rails3_root and run `bundle install`
4. run script/rails generate email_spec:steps